# Optional: base URL of the backend FastAPI server (no trailing slash).
#
# For **web preview (Vite in browser)** prefer VITE_API_BASE_URL because Vite only exposes VITE_* vars to the client bundle:
# VITE_API_BASE_URL=https://vscode-internal-xxxxx:3010
#
# For **Electron / custom injection** you can also use:
# API_BASE_URL=https://vscode-internal-xxxxx:3010
VITE_API_BASE_URL=
API_BASE_URL=
